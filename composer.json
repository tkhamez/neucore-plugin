{
    "name": "tkhamez/neucore-plugin",
    "description": "Necessary classes to write a Neucore plugin.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^8.0.2",
        "ext-json": "*",
        "psr/http-client": "^1.0",
        "psr/http-message": "^1.0",
        "psr/log": "^1.1 || ^2.0 || ^3.0",
        "symfony/yaml": "^6.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "autoload": {
        "psr-4": {
            "Neucore\\Plugin\\": "src/",
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "test:coverage": "XDEBUG_MODE=coverage phpunit --coverage-html coverage"
    },
    "config": {
        "platform": {
            "php": "8.0.2"
        },
        "sort-packages": true
    }
}
